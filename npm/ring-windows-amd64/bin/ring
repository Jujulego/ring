#!/usr/bin/env node

const path = require('path');
const child_process = require('child_process');

child_process.spawnSync(path.join(__dirname, 'ring.exe'), process.argv.slice(2), { stdio: 'inherit' });