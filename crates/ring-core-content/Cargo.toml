[package]
name = "ring-core-content"
version = "0.1.0"
edition = "2024"

[features]
crossterm = ["dep:crossterm"]

[dependencies]
rgb = "0.8.50"
tracing = "0.1.41"

crossterm = { version = "0.29.0", optional = true }