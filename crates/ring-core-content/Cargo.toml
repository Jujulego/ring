[package]
name = "ring-core-content"
version = "0.1.0"
edition = "2024"

[features]
crossterm = ["dep:crossterm"]

[dependencies]
rgb = { version = "0.8.50", features = ["serde"] }
serde = { version = "1.0.219", features = ["derive"] }

crossterm = { version = "0.29.0", optional = true }