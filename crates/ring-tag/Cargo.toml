[package]
name = "ring-tag"
version = "0.1.0"
edition = "2021"

[features]
owo = ["dep:ansi_colours", "dep:owo-colors", "dep:supports-color"]

[dependencies]
ansi_colours = { version = "1.2.3", optional = true }
owo-colors = { version = "4.1.0", optional = true }
supports-color = { version = "3.0.2", optional = true }
rgb = "0.8.50"

[dev-dependencies]
mockall = "0.13.1"