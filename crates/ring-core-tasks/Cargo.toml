[package]
name = "ring-core-tasks"
version = "0.1.0"
edition = "2024"

[features]
crossterm = ["dep:crossterm"]

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
sysinfo = "0.35.0"

crossterm = { version = "0.29.0", optional = true }

ring-core-units = { path = "../ring-core-units" }