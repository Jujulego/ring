[package]
name = "ring-core-units"
version = "0.1.0"
edition = "2024"

[features]
crossterm = ["dep:crossterm", "ring-core-content/crossterm"]

[dependencies]
crossterm = { version = "0.29.0", optional = true }
serde = { version = "1.0.219", features = ["derive"] }

ring-core-content = { path = "../ring-core-content" }
