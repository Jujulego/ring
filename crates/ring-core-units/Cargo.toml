[package]
name = "ring-core-units"
version = "0.1.0"
edition = "2024"

[features]
crossterm = ["dep:crossterm", "ring-core-file/crossterm"]

[dependencies]
crossterm = { version = "0.29.0", optional = true }

ring-core-file = { path = "../ring-core-file" }
