component_management:
  default_rules:
    statuses:
      - type: project
        target: auto

      - type: patch
        target: auto

  individual_components:
    - component_id: cli
      name: cli
      paths:
        - crates/ring-cli/**
        - crates/ring-cli-list/**

    - component_id: core
      name: core
      paths:
        - crates/ring-core-language/**